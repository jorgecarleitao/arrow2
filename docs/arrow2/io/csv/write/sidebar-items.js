initSidebarItems({"fn":[["new_serializer","Returns a [`StreamingIterator`] that yields `&[u8]` serialized from `array` according to `options`. For numeric types, this serializes as usual. For dates, times and timestamps, it uses `options` to Supported types:"],["serialize","Serializes a [`RecordBatch`] as vector of `ByteRecord`. The vector is guaranteed to have `batch.num_rows()` entries. Each `ByteRecord` is guaranteed to have `batch.num_columns()` fields."],["write_batch","Writes the data in a `RecordBatch` to `writer` according to the serialization options `options`."],["write_header","Writes a header to `writer` according to `schema`"]],"struct":[["ByteRecord","A single CSV record stored as raw bytes."],["SerializeOptions","Options to serialize logical types to CSV The default is to format times and dates as `chrono` crate formats them."],["Writer","A already configured CSV writer."],["WriterBuilder","Builds a CSV writer with various configuration knobs."]]});